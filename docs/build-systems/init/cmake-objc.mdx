---
title: Creando un Proyecto Básico en Objective-C con CMake
---
import BoxedTabs from '@site/src/components/cajitas/BoxedTabs'
import TabItem from '@theme/TabItem'
import Explanation from '@site/src/components/admonitions/Explanation'

## 🛠️ Paso -1: Instalar un compilador Objective-C moderno

Para compilar código Objective-C, necesitaremos un compilador compatible. En este curso utilizaremos **Clang**, un compilador moderno basado en el proyecto **LLVM**.

[LLVM](https://llvm.org/) es una infraestructura de compilación modular y extensible que facilita la creación de bibliotecas portables y herramientas de análisis.  
[Clang](https://clang.llvm.org/) es el compilador de C, C++ y Objective-C en este ecosistema.

Se destaca por:

- Mensajes de error **claros y precisos**.
- Excelente soporte **multiplataforma**.
- Integración con herramientas modernas como **CMake**.

A continuación, te mostramos cómo instalar Clang según tu sistema operativo:

<BoxedTabs groupId={"os"}>
    <TabItem value="Windows" label="Windows">
        ```powershell
        scoop install llvm
        ```
    </TabItem>
    <TabItem value="macOS" label="macOS">
        ```bash
        xcode-select --install
        ```
    </TabItem>
    <TabItem value="Linux" label="Linux">
        ```bash
        echo "🔄 Actualizando paquetes..." && \
        sudo apt update && \
        echo "📦 Instalando Clang..." && \
        sudo apt install -y clang libobjc-11-dev
        ```
    </TabItem>
</BoxedTabs>

### ✅ Verificar la instalación

```bash
clang --version
```

Esto asegura que tu entorno esté listo para compilar código Objective-C usando Clang.

## 🧱 Paso 0: Instalar CMake

CMake es una herramienta de construcción multiplataforma que permite definir **cómo compilar y enlazar** programas escritos en C, C++ y Objective-C mediante archivos de configuración portables llamados `CMakeLists.txt`. En este curso lo utilizaremos para **automatizar la compilación de bibliotecas y aplicaciones en Objective-C**, facilitando la integración entre módulos, pruebas, documentación y distribución.

<BoxedTabs groupId={"os"}>
    <TabItem value="Windows" label="Windows">
        ```powershell
        scoop install cmake
        ```
    </TabItem>
    <TabItem value="macOS" label="macOS">
        ```bash
        brew install cmake
        ```

        Reinicia la terminal para aplicar los cambios.
    </TabItem>
    <TabItem value="Linux" label="Linux">
        ```bash
        sudo apt update && \
        sudo apt install -y cmake
        ```
    </TabItem>
</BoxedTabs>

### ✅ Verificar instalación

Ejecuta el siguiente comando para confirmar que CMake se instaló correctamente:

```bash
cmake --version
```

Deberías ver una salida similar a esta:

```plaintext
cmake version 3.22.1
CMake suite maintained and supported by Kitware (kitware.com/cmake)
```

## 📁 Paso 1: Crear el Directorio del Proyecto

Crea una carpeta para tu proyecto y accede a ella desde la terminal o consola:

<BoxedTabs groupId={"os"}>
    <TabItem value="Windows" label="Windows">
        ```powershell
        'echo-app-objc-cmake' | ForEach-Object {
            New-Item -Path $_ -ItemType 'Directory' -Force && `
            Set-Location -Path $_
        }
        ```

        <Explanation>
            Este bloque crea una carpeta llamada `'echo-app-objc-cmake'` y entra en ella.

            - `ForEach-Object` permite reutilizar el nombre dentro del bloque.  
            - `New-Item -ItemType 'Directory' -Force` crea la carpeta, evitando errores si ya existe.  
            - `Set-Location` cambia al nuevo directorio.

            La variable `$_` representa el valor actual en el pipeline, en este caso, `'echo-app-objc-cmake'`.
        </Explanation>
    </TabItem>

    <TabItem value="Windows (corto)" label="Windows (corto)">
        ```powershell
        'echo-app-objc-cmake' | % { md $_ -f && cd $_ }
        ```

        <Explanation>
        Versión abreviada utilizando alias:

        - `%` es un alias de `ForEach-Object`.  
        - `md` es un alias de `New-Item -ItemType Directory`.  
        - `-f` evita errores si la carpeta ya existe. 

        Esta variante es útil si estás en PowerShell con configuración compatible.  
        </Explanation>
    </TabItem>

    <TabItem value="macOS/Linux" label="macOS/Linux">
        ```bash
        name="echo-app-objc-cmake" && \
        mkdir -p "$name" && \
        cd "$name"
        ```

        <Explanation>
            Este comando crea el directorio `echo-app-objc-cmake` y entra en él:

            - `mkdir -p` evita errores si la carpeta ya existe.  
            - `cd` cambia al directorio.  
            - El operador `&&` asegura que `cd` solo se ejecute si `mkdir` fue exitoso.
        </Explanation>
    </TabItem>
</BoxedTabs>

## ⚙️ Paso 2: Crear el archivo de configuración `CMakeLists.txt`

En el directorio raíz del proyecto, crea un archivo llamado `CMakeLists.txt`. Este archivo le dice a CMake cómo compilar tu programa en Objective-C. Abre el archivo y añade la siguiente configuración mínima:

```cmake title="CMakeLists.txt"
cmake_minimum_required(VERSION 3.10)
project(EchoAppObjC)

enable_language(OBJC)
add_executable(echo main.m)
```

<Explanation>
    - `cmake_minimum_required(VERSION 3.10)`: Define la versión mínima de CMake requerida.
    - `project(EchoAppObjC)`: Establece el nombre del proyecto.
    - `enable_language(OBJC)`: Habilita el soporte para Objective-C.
    - `add_executable(echo main.m)`: Declara que el archivo `main.m` se compilará como un ejecutable llamado `echo`.

    Este archivo será la base para compilar tu código con CMake, y lo iremos ampliando en los próximos pasos para incorporar bibliotecas, pruebas, tareas y más.
</Explanation>
