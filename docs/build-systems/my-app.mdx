---
title: Mi primera aplicación
---

import ReadingTime from "../../src/components/ReadingTime";

<ReadingTime/>

En esta sección, aprenderemos a crear una aplicación ejecutable utilizando Kotlin y Gradle. Al igual que en las
secciones anteriores, inicialmente nos conformaremos con una configuración simple para comprender los conceptos básicos
e iremos mejorando la aplicación en las secciones siguientes.

## Echo App

La aplicación que crearemos se llama **Echo App**. Su función es simple: recibir un mensaje como argumento y mostrarlo
en la consola junto a la hora actual. Aunque la funcionalidad es básica, nos permitirá explorar cómo configurar un
proyecto Gradle y ejecutar una aplicación Kotlin.

### Dependencias

Para comenzar, necesitamos expandir las dependencias de nuestro proyecto para añadir `kotlinx-datetime`, una biblioteca
que nos permitirá trabajar con fechas y horas en Kotlin. Para ello, comienza por actualizar el archivo
`libs.versions.toml` en la carpeta `gradle` con la siguiente dependencia:

```toml title="./gradle/libs.versions.toml"
[versions]

kotlinx-datetime = "0.2.1"
```