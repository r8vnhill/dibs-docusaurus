---
title: Clases
slug: /lessons
---
import LessonItem from '@site/src/components/LessonItem'

Cada clase tiene una duración estimada de 90 minutos.

export const lessons = [
    {
        name: "Fundamentos de tipos y representaciones de datos",
        description: "Les presento a Kotlin",
        link: "https://1drv.ms/p/c/7ce753e7fca2208c/EZQVW5q2VclIlZU2urfcof8BckzUg5XjVtTtFfeWz4EfKQ?e=w7deGb"
    },
];

<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 p-6">
    {lessons.map((lesson, index) => (
        <LessonItem
            Url={lesson.link}
            Title={lesson.name}
            Counter={index + 1}
            Description={lesson.description}
            key={index}
        />
    ))}
</div>
