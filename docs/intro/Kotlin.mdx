---
title: "Kotlin: Un lenguaje pragmático y moderno"
---
import ReadingTime from '@site/src/components/ReadingTime'
import { ProCons, Pros, Cons } from '@site/src/components/cajitas/ProCons'
import References from '@site/src/components/ReferencesComponent'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import Exercise from '@site/src/components/exercise/Exercise'
import Solution from '@site/src/components/exercise/Solution'
import Hint from '@site/src/components/exercise/Hint'
import Definition from '@site/src/components/Definition'
import Corollary from '@site/src/components/Corollary'
import GithubRepoLink from '@site/src/components/GithubRepoLink'
import { LanguageCard } from '@site/src/components/cards/LanguageCard'
import Explanation from '@site/src/components/admonitions/Explanation'
import RepoClone from '@site/src/components/admonitions/RepoClone'
import BoxedTabs from '@site/src/components/cajitas/BoxedTabs'

<ReadingTime />
<GithubRepoLink user="r8vnhill" repo="intro-kt" key="intro-kt" />

<img src="/img/kotlin.jpg" alt="El gato se llama Kotlin" class="object-contain h-10 mx-auto" />

Kotlin es un lenguaje moderno que se caracteriza por su pragmatismo, claridad y enfoque en la seguridad. En los últimos años ha ganado popularidad por ofrecer una combinación efectiva de programación orientada a objetos y programación funcional, todo dentro de una sintaxis concisa y expresiva.

En este curso exploraremos Kotlin como herramienta para el **diseño e implementación de bibliotecas de software reutilizables**. Sus características —como la interoperabilidad con Java, la seguridad frente a valores nulos, las funciones de extensión y las coroutines— lo convierten en una opción ideal para construir componentes modulares, mantenibles y multiplataforma.

Esta primera lección es una **introducción al lenguaje y su filosofía**. Veremos cómo luce Kotlin, en qué se diferencia de otros lenguajes, y qué ventajas ofrece a quienes buscan escribir código expresivo y pragmático desde el comienzo. A través de ejemplos y comparaciones, comenzaremos a construir las bases sobre las que se apoyará todo el curso.

## 😺 ¿Qué es Kotlin?

**Kotlin** es un lenguaje de programación multiplataforma desarrollado por **JetBrains** que combina características de la programación orientada a objetos y funcional. Se destaca por su sintaxis concisa y su capacidad para compilar en múltiples plataformas:

- **JavaScript (JS)** y **WebAssembly (WASM)** para la ejecución en navegadores,
- **Java Virtual Machine (JVM)** para servidores y aplicaciones Android,
- **LLVM** para aplicaciones de escritorio y sistemas embebidos.

En este curso, nos centraremos en **Kotlin for the JVM**, que es la plataforma más utilizada para este lenguaje. Sin embargo, los conceptos y técnicas que aprenderás son aplicables a otras plataformas que soporta Kotlin y también te serán útiles en otros lenguajes de programación modernos.

## 🚀 A Taste of Kotlin

A continuación, te presentamos un ejemplo sencillo en Kotlin para que puedas familiarizarte con su sintaxis y estilo:

```kotlin showLineNumbers
data class Person(
    val name: String,
    val age: Int? = null
)

fun main() {
    val persons = listOf(
        Person("Harrier Du Bois"),
        Person("Kim Kitsuragi", age = 43)
    )
    val youngest = persons.minByOrNull { it.age ?: Int.MAX_VALUE }
    println("The youngest is: $youngest")
}
// Output: The youngest is: Person(name=Kim Kitsuragi, age=43)
```

<Explanation>
    - **[1]** Se declara un **registro** (`data class`) llamada `Person` con dos propiedades: `name` de tipo `String` y `age` de tipo `Int?` (opcional).
    - **[3]** La propiedad `age` tiene un valor predeterminado de `null`.
    - **[7-10]** Se crea una lista inmutable (``listOf``) de personas con dos elementos.
    - **[9]** El segundo elemento de la lista tiene un valor nombrado para `age` de 43.
    - **[11]** Se utiliza `minByOrNull` junto con el operador de elvis (`?:`) para encontrar la persona más joven de la lista.
    - **[12]** Se imprime el resultado interpolando la variable `youngest` en una cadena de texto.
</Explanation>

## 📜 Filosofía de Kotlin

Kotlin se diseñó con la intención de **hacer la programación más sencilla y segura**, sin sacrificar flexibilidad ni potencia. Algunos de sus principios fundamentales son:

- **Pragmático**: Diseñado para ser práctico y resolver problemas del mundo real de manera eficiente.
- **Conciso y legible**: Su sintaxis está pensada para ser simple y fácil de leer, eliminando la verbosidad innecesaria.
- **Seguro**: Enfocado en prevenir errores comunes, como las referencias a valores nulos, mejorando la seguridad en tiempo de compilación.
- **Explícito**: Fomenta la claridad en la intención del código, permitiendo que lxs desarrolladorxs expresen sus ideas de manera directa y sin ambigüedades.

## ⚖️ Beneficios y limitaciones de Kotlin en el desarrollo de bibliotecas de software

<ProCons>
    <Pros>
        - **Interoperabilidad con Java** → Kotlin puede trabajar con código Java sin problemas, permitiendo el uso de bibliotecas existentes sin esfuerzo adicional.  
        - **Sintaxis concisa y expresiva** → Reduce el código repetitivo, haciendo que las bibliotecas sean más legibles y fáciles de mantener.  
        - **Seguridad frente a nulls** → Previene `NullPointerExceptions` en tiempo de compilación, lo que mejora la estabilidad de las bibliotecas.  
        - **Funciones de extensión** → Permiten agregar funcionalidades sin modificar el código original, ideal para extender bibliotecas existentes.  
        - **Compatibilidad multiplataforma** → Facilita el desarrollo de bibliotecas reutilizables en diferentes entornos.  
        - **Coroutines** → Soporta programación asíncrona de forma más simple y eficiente.  
        - **Facilidad para crear DSLs** → Kotlin permite construir APIs más intuitivas y expresivas.  
    </Pros>
    <Cons>
        - **Falta de funcionalidades avanzadas** → A pesar de su flexibilidad, Kotlin carece de algunas características más avanzadas y expresivas que se encuentran en otros lenguajes, como los **implicits** en Scala o el soporte completo para **Higher-Kinded Types (HKT)**.  
        No obstante, existe una propuesta en curso para introducir **[context parameters](https://github.com/Kotlin/KEEP/blob/context-parameters/proposals/context-parameters.md)**, que permitirían un comportamiento similar al de los implicits, abriendo la puerta a nuevas formas de diseño funcional y reutilizable en bibliotecas Kotlin.
        - **Menor adopción en comparación con Java** → Aunque está creciendo rápidamente, algunas empresas siguen prefiriendo Java, lo que puede afectar la adopción de bibliotecas en Kotlin.  
        - **Curva de aprendizaje en paradigmas modernos** → Algunas características como las **coroutines** pueden ser nuevas para quienes vienen de lenguajes más tradicionales.  
        - **Variabilidad en la compatibilidad multiplataforma** → No todas las bibliotecas funcionan igual en JVM, JS y Native, lo que a veces requiere ajustes adicionales.  
        - **Ecosistema aún en crecimiento** → Aunque muchas bibliotecas populares tienen soporte en Kotlin, la cantidad sigue siendo menor en comparación con Java.  
    </Cons>
</ProCons>

## 🧮 Expresiones vs. Declaraciones

Es importante distinguir entre **expresiones** y **declaraciones**, ya que cumplen roles distintos en la estructura de un programa. En Kotlin, esta diferencia es clara y ayuda a escribir código más seguro y predecible. En cambio, en otros lenguajes como Scala o Rust, la distinción puede ser más difusa.

<Definition title="Expresiones">
    Las **expresiones** son fragmentos de código que **producen un valor** y pueden combinarse con otras expresiones. En Kotlin, una expresión puede ser tan simple como una constante, o tan compleja como una función anónima. Ejemplos comunes son las operaciones aritméticas, los operadores lógicos y las llamadas a funciones.
</Definition>

<Definition title="Declaraciones">
    Las **declaraciones** ejecutan una acción y, por lo general, **no se utilizan por su valor**. A diferencia de las expresiones, no pueden combinarse con otras expresiones. Ejemplos en Kotlin incluyen la asignación de variables, la ejecución de bucles y la definición de funciones.
</Definition>

Una diferencia clave con respecto a **lenguajes como Java** es que en ellos **las asignaciones son expresiones**, lo que permite escribir cosas como `if (x = 5)`, un error común si se pretendía comparar en lugar de asignar.  
En cambio, **en Kotlin las asignaciones son declaraciones**, lo que evita este tipo de confusión y ayuda a detectar errores en tiempo de compilación.

| Ejemplo                     | ¿Expresión o declaración? | ¿Devuelve valor?        |
| --------------------------- | ------------------------- | ----------------------- |
| `val x = 5`                 | Declaración               | No                      |
| `5`                         | Expresión                 | Sí (`5`)                |
| `if (a > b) a else b`       | Expresión                 | Sí (valor de `a` o `b`) |
| `fun greet() { println() }` | Declaración               | No                      |

En resumen, **las expresiones devuelven un valor que puede usarse**, mientras que **las declaraciones ejecutan acciones** y no se combinan con otras expresiones. Esta distinción clara en Kotlin contribuye a escribir programas más seguros, especialmente en contextos donde errores sutiles (como confundir `=` con `==`) podrían pasar desapercibidos.

## 🔧 Creando un proyecto Kotlin

Antes de comenzar con ejemplos concretos, es importante tener un entorno de desarrollo funcional. A continuación se muestra cómo **crear un proyecto básico de Kotlin** utilizando Gradle desde la terminal.

Te proporcionamos instrucciones específicas para Windows, Linux y macOS, adaptadas a los comandos y alias comunes en cada sistema operativo:

<BoxedTabs groupId={"os"}>
    <TabItem value="Windows" label="Windows">
        ```powershell
        "intro-kt" | ForEach-Object {
            New-Item -Path $_ -ItemType Directory -Force && `
                Set-Location $_
        } && gradle init --use-defaults --type kotlin-application
        ```

        <Explanation>
            Este bloque de PowerShell automatiza la creación de un proyecto Kotlin utilizando Gradle:

            1. **`"intro-kt" | ForEach-Object { ... }`**: Usa un pipeline para crear y entrar a un directorio llamado `intro-kt`. Aunque aquí se usa un solo nombre, este patrón permite trabajar con múltiples nombres si fuera necesario.
            2. **`New-Item -Path $_ -ItemType Directory -Force`**: Crea el directorio. `-Force` evita errores si el directorio ya existe.
            3. **`Set-Location $_`**: Cambia al nuevo directorio.
            4. **`&& gradle init --use-defaults --type kotlin-application`**: Inicializa un proyecto Gradle como aplicación Kotlin, con configuración por defecto.
        </Explanation>
    </TabItem>

    <TabItem value="Windows (corto)" label="Windows (corto)">
        
        ```powershell
        "intro-kt" | % { md $_ -f && cd $_ } && `
            gradle init --use-defaults --type kotlin-application
        ```

        <Explanation>
            Versión abreviada del mismo proceso usando alias comunes en PowerShell:

            1. **`%`** es un alias de `ForEach-Object`, **`md`** es un alias de `New-Item -ItemType Directory`, y **`-f`** equivale a `-Force`.
            2. Este bloque:
                - Crea el directorio `intro-kt`.
                - Entra en él con `cd $_`.
            3. Si todo sale bien (`&&`), se inicializa un proyecto Kotlin con Gradle.
        </Explanation>
    </TabItem>
    <TabItem value="Linux/macOS" label="Linux/macOS">
        ```bash
        dir="intro-kt" && mkdir -p $dir && cd $dir && \
            gradle init --use-defaults --type kotlin-application
        ```

        <Explanation>
            Este comando en Bash automatiza la creación de un proyecto Kotlin con Gradle:

            1. **`dir="intro-kt"`**: Define el nombre del nuevo proyecto.
            2. **`mkdir -p $dir`**: Crea el directorio (sin fallar si ya existe).
            3. **`cd $dir`**: Entra al directorio.
            4. **`gradle init --use-defaults --type kotlin-application`**: Inicializa un proyecto de aplicación básica con estructura Kotlin.
        </Explanation>
    </TabItem>
</BoxedTabs>

Por ahora, crearemos una **aplicación de consola** para probar nuestro código. Más adelante exploraremos cómo utilizar Gradle para construir **bibliotecas de software** reutilizables.

Una vez creado el proyecto, puedes abrirlo con IntelliJ IDEA, Fleet u otro editor. No es necesario que entiendas aún toda su estructura, pero es útil saber que:

- El código fuente está en `app/src/main/kotlin`
- Las pruebas están en `app/src/test/kotlin`

Si usas IntelliJ IDEA y tienes la herramienta `idea` disponible en tu terminal, puedes abrir el proyecto con:

```bash
idea .
```

<Explanation>
    Este comando abre el proyecto actual en IntelliJ IDEA, siempre que tengas instalado el [Command Line Launcher](https://www.jetbrains.com/help/idea/working-with-the-ide-features-from-command-line.html) (`idea`) desde el menú `Tools > Create Command-line Launcher`.
</Explanation>

## 🎯 Conclusiones

Kotlin se consolida como un lenguaje moderno, pragmático y expresivo, ideal para el desarrollo de **bibliotecas de software reutilizables, seguras y mantenibles**. Su diseño promueve la claridad y la concisión, sin sacrificar robustez ni interoperabilidad con el ecosistema Java.

Uno de los aspectos más distintivos que vimos es la **diferenciación entre expresiones y declaraciones**, una distinción fundamental en Kotlin que permite construir funciones y estructuras de control más limpias, componibles y predecibles.

Aunque aún enfrenta desafíos en compatibilidad multiplataforma y en la adopción de ciertas abstracciones avanzadas, ofrece un conjunto de herramientas accesible y potente para quienes buscan crear componentes modulares con una experiencia de desarrollo fluida.

### 🔑 Puntos clave

- Kotlin diferencia claramente entre **expresiones** (que devuelven un valor) y **declaraciones** (que ejecutan una acción), lo que facilita escribir código más conciso y reutilizable.
- Combina paradigmas **orientado a objetos** y **funcional**, permitiendo diseñar bibliotecas expresivas y bien estructuradas.
- Su enfoque en **la seguridad frente a nulls**, la **concisión sintáctica** y las **funciones de extensión** mejora notablemente la calidad del código.
- La interoperabilidad con Java facilita la integración de bibliotecas existentes y la adopción progresiva del lenguaje.
- Las **coroutines**, los **tipos de datos expresivos** y el soporte para **DSLs** lo hacen especialmente atractivo para bibliotecas modernas.

### 🧰 ¿Qué nos llevamos?

Más allá de sus herramientas o sintaxis, Kotlin nos propone una **forma de pensar el diseño de software**. Nos invita a escribir código **claro, seguro y expresivo** desde el primer día, facilitando el desarrollo de bibliotecas elegantes, idiomáticas y fáciles de mantener.

Esta primera lección nos deja una base conceptual sólida que usaremos a lo largo del curso para explorar las verdaderas fortalezas de Kotlin en el desarrollo de componentes reutilizables, robustos y agradables de usar.

## 📖 Referencias

### 🔥 Recomendadas

- 📚 Kotlin basics. (2017). En Dmitry Jemerov & Svetlana Isakova, Kotlin in action (pp. 17–43). Manning Publications Co.
- 📚 Kotlin: What and why. (2017). En Dmitry Jemerov & Svetlana Isakova, Kotlin in action (pp. 3–16). Manning Publications Co.

### 🔹 Adicionales

- 🌐 Kotlin (programming language). (2025). En Wikipedia. https://en.wikipedia.org/w/index.php?title=Kotlin_(programming_language)&oldid=1282501713
