---
title: "Kotlin: Un lenguaje pragmático y moderno"
---
import ReadingTime from '@site/src/components/ReadingTime'
import { ProCons, Pros, Cons } from '@site/src/components/cajitas/ProCons'
import References from '@site/src/components/ReferencesComponent'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import Exercise from '@site/src/components/exercise/Exercise'
import Solution from '@site/src/components/exercise/Solution'
import Hint from '@site/src/components/exercise/Hint'
import Definition from '@site/src/components/Definition'
import Corollary from '@site/src/components/Corollary'
import GithubRepoLink from '@site/src/components/GithubRepoLink'
import { LanguageCard } from '@site/src/components/cards/LanguageCard'
import Explanation from '@site/src/components/admonitions/Explanation'
import RepoClone from '@site/src/components/admonitions/RepoClone'

<ReadingTime />
<GithubRepoLink user="r8vnhill" repo="" />

<img src="/img/kotlin.jpg" alt="El gato se llama Kotlin" class="object-contain h-10 mx-auto" />

**Kotlin** es un lenguaje de programación multiplataforma desarrollado por **JetBrains** que combina características de la programación orientada a objetos y funcional. Se destaca por su sintaxis concisa y su capacidad para compilar en múltiples plataformas:

- **JavaScript (JS)** y **WebAssembly (WASM)** para la ejecución en navegadores,
- **Java Virtual Machine (JVM)** para servidores y aplicaciones Android,
- **LLVM** para aplicaciones de escritorio y sistemas embebidos.

En este curso, nos centraremos en **Kotlin for the JVM**, que es la plataforma más utilizada para este lenguaje. Sin embargo, los conceptos y técnicas que aprenderás son aplicables a otras plataformas que soporta Kotlin y también te serán útiles en otros lenguajes de programación modernos.

## 🚀 A Taste of Kotlin

A continuación, te presentamos un ejemplo sencillo en Kotlin para que puedas familiarizarte con su sintaxis y estilo:

```kotlin showLineNumbers
data class Person(
    val name: String,
    val age: Int? = null
)

fun main() {
    val persons = listOf(
        Person("Harrier Du Bois"),
        Person("Kim Kitsuragi", age = 43)
    )
    val youngest = persons.minByOrNull { it.age ?: Int.MAX_VALUE }
    println("The youngest is: $youngest")
}
// Output: The youngest is: Person(name=Kim Kitsuragi, age=43)
```

<Explanation>
- **[1]** Se declara un **registro** (`data class`) llamada `Person` con dos propiedades: `name` de tipo `String` y `age` de tipo `Int?` (opcional).
- **[3]** La propiedad `age` tiene un valor predeterminado de `null`.
- **[7-10]** Se crea una lista inmutable (``listOf``) de personas con dos elementos.
- **[9]** El segundo elemento de la lista tiene un valor nombrado para `age` de 43.
- **[11]** Se utiliza `minByOrNull` junto con el operador de elvis (`?:`) para encontrar la persona más joven de la lista.
- **[12]** Se imprime el resultado interpolando la variable `youngest` en una cadena de texto.
</Explanation>

## 📜 Filosofía de Kotlin

Kotlin se diseñó con la intención de **hacer la programación más sencilla y segura**, sin sacrificar flexibilidad ni potencia. Algunos de sus principios fundamentales son:

- **Pragmático**: Diseñado para ser práctico y resolver problemas del mundo real de manera eficiente.
- **Conciso y legible**: Su sintaxis está pensada para ser simple y fácil de leer, eliminando la verbosidad innecesaria.
- **Seguro**: Enfocado en prevenir errores comunes, como las referencias a valores nulos, mejorando la seguridad en tiempo de compilación.
- **Explícito**: Fomenta la claridad en la intención del código, permitiendo que lxs desarrolladorxs expresen sus ideas de manera directa y sin ambigüedades.

## ⚖️ Beneficios y limitaciones de Kotlin en el desarrollo de bibliotecas de software

<ProCons>
    <Pros>
        - **Interoperabilidad con Java** → Kotlin puede trabajar con código Java sin problemas, permitiendo el uso de bibliotecas existentes sin esfuerzo adicional.  
        - **Sintaxis concisa y expresiva** → Reduce el código repetitivo, haciendo que las bibliotecas sean más legibles y fáciles de mantener.  
        - **Seguridad frente a nulls** → Previene `NullPointerExceptions` en tiempo de compilación, lo que mejora la estabilidad de las bibliotecas.  
        - **Funciones de extensión** → Permiten agregar funcionalidades sin modificar el código original, ideal para extender bibliotecas existentes.  
        - **Compatibilidad multiplataforma** → Facilita el desarrollo de bibliotecas reutilizables en diferentes entornos.  
        - **Coroutines** → Soporta programación asíncrona de forma más simple y eficiente.  
        - **Facilidad para crear DSLs** → Kotlin permite construir APIs más intuitivas y expresivas.  
    </Pros>
    <Cons>
        - **Falta de funcionalidades avanzadas** → A pesar de su flexibilidad, Kotlin carece de algunas características más avanzadas y experimentales que se encuentran en otros lenguajes, como los **implicits** en Scala o el soporte completo para **Higher-Kinded Types (HKT)**. Esto limita las posibilidades para diseñar bibliotecas con conceptos de abstracción más sofisticados.
        - **Menor adopción en comparación con Java** → Aunque está creciendo rápidamente, algunas empresas siguen prefiriendo Java, lo que puede afectar la adopción de bibliotecas en Kotlin.  
        - **Curva de aprendizaje en paradigmas modernos** → Algunas características como las **coroutines** pueden ser nuevas para quienes vienen de lenguajes más tradicionales.  
        - **Variabilidad en la compatibilidad multiplataforma** → No todas las bibliotecas funcionan igual en JVM, JS y Native, lo que a veces requiere ajustes adicionales.  
        - **Ecosistema aún en crecimiento** → Aunque muchas bibliotecas populares tienen soporte en Kotlin, la cantidad sigue siendo menor en comparación con Java.  
    </Cons>
</ProCons>

## 💻 Ejecutando el código de esta unidad

Para probar el código sin necesidad de configurar un entorno de desarrollo local, puedes utilizar [Kotlin Playground](https://play.kotlinlang.org/).  
En la siguiente unidad, exploraremos cómo configurar un entorno de desarrollo adecuado.

## 🔜 Próximos pasos en esta unidad

En las siguientes lecciones, haremos un repaso general sobre los conceptos básicos de Kotlin. El objetivo es proporcionar una referencia rápida para quienes ya tienen experiencia con estos temas en otros lenguajes. Si prefieres, puedes saltarte estas lecciones y continuar con la siguiente unidad, manteniéndolas solo como referencia para futuras consultas rápidas. Sin embargo, se recomienda hacer una pasada rápida para asegurarte de que estás familiarizado con la sintaxis y las características clave de Kotlin.

Algunas lecciones que recomendamos revisar son:

- [🔢 Tipos de funciones](/docs/intro/types)  
- [🛑 Void-safety](/docs/intro/void/void-safety)  

Para la mayoría de los conceptos, se ofrecen ejemplos en otros lenguajes de programación para ayudarte a comparar y contrastar con lo que ya conoces. Esto te permitirá identificar similitudes y diferencias, lo que facilitará tu aprendizaje y te ayudará a comprender mejor los conceptos, además de proporcionarte una visión más amplia de la programación en general.

<References references={[
    {
        title: "Kotlin: What and why",
        bookTitle: "Kotlin in action",
        publisher: "Manning Publications Co.",
        pages: "3–16",
        year: "2017",
        author: "Dmitry Jemerov & Svetlana Isakova",
        type: "book",
    },
]} additionalReferences={[
    
]}/>