import Explanation from '@site/src/components/admonitions/Explanation'

```bash showLineNumbers title="scripts/sh/create_project_structure.sh"
#!/bin/sh

set -e  # Exit immediately if a command exits with a non-zero status

echo "📦 Creating project structure..."

for file in \
    Sources/App/main.swift \
    Sources/Lib/Echo.swift \
    Tests/EchoTests.swift
do
    dir=$(dirname "$file")
    mkdir -p "$dir"
    touch "$file"
    echo "✅ $file"
done

echo "🟢 Project initialized successfully."
```

<Explanation>
    Este script crea automáticamente la estructura inicial de carpetas y archivos para un proyecto Swift modular, compatible con Swift Package Manager (SwiftPM).

    - Usa `set -e` para asegurarse de que el script se detenga ante cualquier error.
    - Define una lista de archivos que deben existir en el proyecto (incluyendo `main.swift`, `Echo.swift`, y `EchoTests.swift`).
    - Para cada archivo:
        - Crea la carpeta contenedora con `mkdir -p` si aún no existe.
        - Crea un archivo vacío con `touch`.
        - Muestra en consola una confirmación (`✅`) por cada archivo creado.
    - Al final, imprime un mensaje que indica que la estructura se ha generado correctamente (`🟢`).

    Este tipo de automatización es útil para mantener consistencia en la estructura del proyecto y evitar errores al crear carpetas o archivos manualmente.
</Explanation>

Puedes crear el archivo `create_project_structure.sh` y ejecutarlo desde la terminal:

```bash
chmod +x scripts/sh/create_project_structure.sh
./scripts/sh/create_project_structure.sh
```