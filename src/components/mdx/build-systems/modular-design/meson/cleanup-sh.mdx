```bash showLineNumbers title="cleanup_project.sh"
#!/bin/sh

echo "🗑️ Removing files..."

for file in src/main.cpp src/meson.build; do
    if [ -f "$file" ]; then
        rm -f "$file"
        echo "✅ Removed: $file"
    else
        echo "⚠️ File not found: $file"
    fi
done
```