/**
 * Interface for Collapsible sections.
 */
export default interface Collapsible {
  /**
   * Whether the section is collapsible.
   */
  collapsible?: boolean;

  /**
   * Whether the section is open by default.
   */
  openByDefault?: boolean;
}
